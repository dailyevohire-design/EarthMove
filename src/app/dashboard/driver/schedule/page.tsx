import { StubPage } from '@/components/driver/dash-v2/StubPage'

export const metadata = { title: 'Schedule + preferences — earthmove.io' }

export default function ScheduleStub() {
  return (
    <StubPage
      eyebrow="Operations · Schedule + preferences"
      title="Set when, where, and what you'll haul."
    />
  )
}
