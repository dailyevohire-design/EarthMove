import { StubPage } from '@/components/driver/dash-v2/StubPage'

export const metadata = { title: 'My documents — earthmove.io' }

export default function DocumentsStub() {
  return (
    <StubPage
      eyebrow="Documents · My documents"
      title="Every document on file, in one place."
    />
  )
}
