import { StubPage } from '@/components/driver/dash-v2/StubPage'

export const metadata = { title: 'Available loads — earthmove.io' }

export default function LoadsStub() {
  return (
    <StubPage
      eyebrow="Operations · Available loads"
      title="The full available-loads queue."
    />
  )
}
